# redis-study
Redis-compatible server written in Go. Implements RESP2, in-memory storage, and a subset of Redis commands including Streams (XADD, XRANGE, XREAD with BLOCK). The project is work in progress — new features and commands are continuously added as part of learning infrastructure-level backend systems, concurrency, and protocol design.
